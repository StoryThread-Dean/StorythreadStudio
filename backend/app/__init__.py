# This file tells Python that the `app` folder is a "package" --
# meaning its contents can be imported like a module from other files.
# Think of it as a label on a filing cabinet drawer that says
# "everything in here belongs together."
#
# It doesn't need any code in it right now. Its presence alone is
# what Python looks for when it processes import statements.
